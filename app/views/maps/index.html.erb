<%= link_to 'sign_out', destroy_user_session_path, :method => :delete %>

<div style='width: 800px;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>

<script src="//maps.google.com/maps/api/js?key=AIzaSyC1AVEutS4l6s_JYfoCjF2Go1F6xSZHq4U"></script>
<script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
<script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->

<script>
	handler = Gmaps.build('Google');
	handler.buildMap({
	    provider: {
	      // disableDefaultUI: false,
	      // pass in other Google Maps API options here
	      center: {lat: parseInt("<%= @user_latitude %>"), lng: parseInt("<%= @user_longitude %>")},
	      minZoom: 0,
	      maxZoom: 18,
	      zoom: 13,
	      // mapTypeId: 'hybrid'
	      mapTypeId: 'roadmap'
	    },
	    internal: {
	      id: 'map'
	    }
	  },
	  function(){
	    markers = handler.addMarkers([
	      {
	        "lat": parseInt("<%= @user_latitude %>"),
	        "lng": parseInt("<%= @user_longitude %>"),
	        "picture": {
	          "url": "http://people.mozilla.com/~faaborg/files/shiretoko/firefoxIcon/firefox-32.png",
	          "width":  32,
	          "height": 32
	        },
	        "infowindow": "YOU ARE HERE"
	      }
	    ]);
	    // handler.bounds.extendWith(markers);
	    // handler.fitMapToBounds();
	  }
	);
</script>


<%= react_component('HelloMessage', name: 'John') %>























